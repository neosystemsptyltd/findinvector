#ifndef FINDINVECTOR_H_INCLUDED
#define FINDINVECTOR_H_INCLUDED

#include <utility>
#include <vector>

template < typename T>
std::pair<bool, int > findInVector(const std::vector<T>  & vecOfElements, const T  & element);

#endif // FINDINVECTOR_H_INCLUDED
